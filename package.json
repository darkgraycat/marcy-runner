{
  "name": "marcy-runner-game",
  "version": "1.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run serve & npm run watch",
    "serve": "npx live-server public --port=8080 --watch=public",
    "watch": "npx esbuild src/main.ts --bundle --outfile=public/main.js --sourcemap=inline --watch",
    "build": "npx esbuild src/main.ts --bundle --outfile=public/main.js --minify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "darkgraycat",
  "license": "ISC",
  "dependencies": {
    "phaser": "^3.87.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.2",
    "typescript": "^5.5.4",
    "live-server": "^1.2.2"
  }
}

{
  "name": "marcy-runner-game",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run serve & npm run watch",
    "serve": "npx live-server --port=8080 --watch=dist,assets,index.html,style.css",
    "watch": "npx esbuild src/main.ts --bundle --outdir=dist --sourcemap=inline --watch",
    "build": "npx esbuild src/main.ts --bundle --minify --outdir=dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "darkgraycat",
  "license": "ISC",
  "dependencies": {
    "phaser": "^3.87.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.2",
    "typescript": "^5.5.4",
    "live-server": "^1.2.2"
  }
}
